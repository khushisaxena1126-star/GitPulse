// After deploying to Render, replace the URL below with your Render service URL.
// Example: window.API_BASE = 'https://gitpulse-api.onrender.com/api'
// Leave as empty string to fall back to localhost (for local development).
window.API_BASE = 'https://gitpulse-api-tznz.onrender.com'
